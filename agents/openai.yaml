interface:
  display_name: "Tiered Agent Orchestrator"
  short_description: "Zero-context handoffs with cost-aware execution"
  default_prompt: "$tao Resume from repository state, keep OWNER_STATUS.md concise, prefer the simplest sufficient mechanism, reuse worker-1, and preserve a gpt-5.6-sol Lead, attested gpt-5.6-luna/xhigh Workers, Terra-first escalation, and passive waits."
policy:
  allow_implicit_invocation: false
