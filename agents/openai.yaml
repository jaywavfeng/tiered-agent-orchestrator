interface:
  display_name: "Tiered Agent Orchestrator"
  short_description: "Reusable workers with lean, repository-backed handoffs"
  default_prompt: "$tao Resume from repository state, reuse Workers, use explicit Python CLI calls, keep coordination below 10% when measured, and follow strict route evidence with passive event waits."
policy:
  allow_implicit_invocation: false
