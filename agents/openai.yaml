interface:
  display_name: "Tiered Agent Orchestrator"
  short_description: "Persistent lead, low-cost workers, shared project state"
  default_prompt: "Use $tiered-agent-orchestrator to plan and execute this large engineering goal with a persistent lead and cost-efficient workers."
policy:
  allow_implicit_invocation: true
